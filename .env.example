# AstraVia — Environment Variables Example
# Copy this to .env.development / .env.staging / .env.production
# NEVER commit the actual .env files (they are in .gitignore)

# ============================================================
# FIREBASE
# ============================================================
# Android google-services.json is NOT stored here.
# It is stored as a CI secret (GOOGLE_SERVICES_JSON = base64 encoded)
FIREBASE_PROJECT_ID=your-firebase-project-id

# ============================================================
# ADMOB
# ============================================================
# Replace with actual AdMob App ID from AdMob Console
ADMOB_APP_ID_ANDROID=ca-app-pub-REPLACE~REPLACE
ADMOB_BANNER_UNIT_ID=ca-app-pub-REPLACE/REPLACE
ADMOB_INTERSTITIAL_UNIT_ID=ca-app-pub-REPLACE/REPLACE

# Use test IDs during development:
# ADMOB_APP_ID_ANDROID=ca-app-pub-3940256099942544~3347511713
# ADMOB_BANNER_UNIT_ID=ca-app-pub-3940256099942544/6300978111
# ADMOB_INTERSTITIAL_UNIT_ID=ca-app-pub-3940256099942544/1033173712

# ============================================================
# GOOGLE PLAY BILLING (Product IDs — match Play Console)
# ============================================================
PLAY_SUBSCRIPTION_MONTHLY_ID=premium_monthly_v1
PLAY_SUBSCRIPTION_YEARLY_ID=premium_yearly_v1

# ============================================================
# ANDROID SIGNING (CI only — never in local .env)
# ============================================================
# KEYSTORE_BASE64=<base64 encoded .jks file>
# KEYSTORE_PASSWORD=<store password>
# KEY_PASSWORD=<key password>
# KEY_ALIAS=astravia-key

# ============================================================
# FEATURE FLAGS (can also use Firebase Remote Config)
# ============================================================
AD_SHOW_AFTER_DAYS=7
FREE_HISTORY_DAYS=7
PREMIUM_HISTORY_DAYS=90
ENABLE_LLM_CONTENT=false
